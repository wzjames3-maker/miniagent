# 骨髓总览 — 这三篇在讲什么“皮下”

入门 `00-11` 让你“能跑、能讲、能改”。这三篇让你“敢改内核”。

- `BONE-01-Agent内核调度`：`core.py:117` 的 `run/query/execute_actions/_finish/doom_loop/interrupt` 五段代码为何这样排，为何 `step_limit` 不在 `Agent` 而在 `DefaultAgent`。
- `BONE-02-Context与缓存`：`context/manager.py:1` 的 docstring“两次压缩间字节级稳定”如何用 `system_offset`/`prepare:309`/`prune:131` 三处代码保证，`tokens.py:29` 的 `3.6/1.3` 估算为何偏保守。
- `BONE-03-Tool与存储的底盘`：`search_tools.py:1` 的 `ripgrep` 快径与 `SKIP_DIR_NAMES:60` 单源、`storage/json_store.py:18` 的 `fsync` 与 `Lock`，以及 `permission/policy.py:147` 的 `*` 不跨 `/` 为何必须双拒。

读法：先看现象，再看 `ARCHITECTURE.md` 对应节，最后逐行对代码。不需要背，只需在改内核时知道去哪一行。

> 约定：所有 `file:line` 均以 `HEAD` 为准，可 `read/grep` 验证；贴出的代码仅为当下必需的最小片段。
